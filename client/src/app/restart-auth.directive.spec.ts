import { RestartAuthDirective } from './restart-auth.directive';

describe('RestartAuthDirective', () => {
  it('should create an instance', () => {
    const directive = new RestartAuthDirective();
    expect(directive).toBeTruthy();
  });
});
